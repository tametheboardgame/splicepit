{
  "name": "splicepit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "SplicePit playable vertical slice",
  "scripts": {
    "dev": "python3 -m http.server 8080",
    "check": "node scripts/check-syntax.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run check && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
